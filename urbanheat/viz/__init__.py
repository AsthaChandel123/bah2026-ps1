"""urbanheat.viz — maps and report generation.

* ``maps`` — render hotspot/driver/intervention layers to interactive
  (leafmap/folium) and static (matplotlib) maps using the canonical legends
  in :mod:`urbanheat.constants`.
* ``report`` — assemble the deliverable report (heat-stress maps, driver
  attribution table, validated-model metrics, scenario ΔT, optimal portfolio).
"""
